<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database Connection Settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <!-- DB_URL, DB_USERNAME and DB_PASSWORD are read from environment variables. -->

        <!-- SQL Dialect for MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo all executed SQL to stdout (Good for debugging) -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Automatically validate/update schema drops/creates tables -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- List entity classes here as you build them -->
        <mapping class="com.pahappa.internship.savingsgroupmangement.model.User"/>
        <mapping class="com.pahappa.internship.savingsgroupmangement.model.Transaction"/>
        <mapping class="com.pahappa.internship.savingsgroupmangement.model.Loan"/>

    </session-factory>
</hibernate-configuration>
